# Monitoring

This directory is reserved for monitoring evidence that is produced after the
cluster exists:

- exported Grafana dashboard JSON,
- screenshots of dashboards and alerts,
- Prometheus query examples,
- alert firing/resolution evidence,
- CloudWatch log verification screenshots.

Desired state for monitoring lives under `platform/monitoring` and
`helm/application/templates`.
